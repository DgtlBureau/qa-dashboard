# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: ../setup/auth.setup.ts >> authenticate as admin
- Location: tests/e2e/setup/auth.setup.ts:6:6

# Error details

```
Error: 2FA verify failed: 401

expect(received).toBeTruthy()

Received: false
```

# Test source

```ts
  1  | import { test as setup, expect } from '@playwright/test';
  2  | import path from 'path';
  3  | 
  4  | const AUTH_FILE = path.resolve(__dirname, '../.auth/admin.json');
  5  | 
  6  | setup('authenticate as admin', async ({ page, request }) => {
  7  |   const apiUrl = process.env.E2E_API_URL;
  8  |   const email = process.env.E2E_ADMIN_EMAIL;
  9  |   const password = process.env.E2E_ADMIN_PASSWORD;
  10 |   const tfaCode = process.env.E2E_2FA_CODE || '111111';
  11 | 
  12 |   if (!apiUrl || !email || !password) {
  13 |     throw new Error(
  14 |       'Missing E2E env vars. Copy tests/e2e/.env.e2e.example to .env.e2e and fill in credentials.'
  15 |     );
  16 |   }
  17 | 
  18 |   // Step 1: Login via API (fast, no browser overhead)
  19 |   const loginResponse = await request.post(`${apiUrl}/login`, {
  20 |     data: { email, password },
  21 |   });
  22 |   expect(loginResponse.ok(), `Login failed: ${loginResponse.status()}`).toBeTruthy();
  23 | 
  24 |   const loginData = await loginResponse.json();
  25 |   let token = loginData.token;
  26 | 
  27 |   // Step 1b: Handle 2FA if required.
  28 |   // Бэкенд Дубая включил двухфакторку: /login отдаёт 200 с requires_2fa и без
  29 |   // token, поэтому прежний деструктуринг падал на «No token in login response».
  30 |   // Контракт совпадает с AuthenticationProvider.verifyTwoFactor в приложении.
  31 |   if (loginData.requires_2fa) {
  32 |     const verifyResponse = await request.post(`${apiUrl}/login/verify-2fa`, {
  33 |       data: { email, password, code: tfaCode },
  34 |     });
> 35 |     expect(verifyResponse.ok(), `2FA verify failed: ${verifyResponse.status()}`).toBeTruthy();
     |                                                                                  ^ Error: 2FA verify failed: 401
  36 |     const verifyData = await verifyResponse.json();
  37 |     token = verifyData.token;
  38 |   }
  39 | 
  40 |   expect(token, 'No token in login response').toBeTruthy();
  41 | 
  42 |   // Step 2: Inject token into localStorage via browser
  43 |   await page.goto('/login', { waitUntil: 'commit' });
  44 |   await page.evaluate((t: string) => {
  45 |     localStorage.setItem('AUTH_TOKEN', t);
  46 |   }, token);
  47 | 
  48 |   // Step 3: Save storageState (localStorage + cookies) for all workers.
  49 |   await page.context().storageState({ path: AUTH_FILE });
  50 | });
  51 | 
```