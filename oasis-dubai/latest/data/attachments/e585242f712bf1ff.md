# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: 04-accommodations-crud.spec.ts >> CRUD: Размещения >> Созданное размещение отображается в списке @smoke
- Location: tests/e2e/specs/04-accommodations-crud.spec.ts:47:7

# Error details

```
Error: expect(locator).toHaveCount(expected) failed

Locator:  locator('tbody tr')
Expected: 1
Received: 0
Timeout:  30000ms

Call log:
  - Expect "toHaveCount" with timeout 30000ms
  - waiting for locator('tbody tr')
    33 × locator resolved to 0 elements
       - unexpected value "0"

```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test';
  2   | import { AccommodationFormPage } from '../pages/accommodation-form.page';
  3   | import { TablePage } from '../pages/table.page';
  4   | import { ApiHelper } from '../helpers/api.helper';
  5   | import { waitForTableData } from '../helpers/wait.helper';
  6   | 
  7   | // CRUD tests run sequentially: create -> verify -> delete
  8   | test.describe.configure({ mode: 'serial' });
  9   | 
  10  | const TEST_PREFIX = '[E2E]';
  11  | const TEST_NAME = `${TEST_PREFIX} Smoke ${Date.now()}`;
  12  | const TEST_ADDRESS = 'E2E Test Address';
  13  | const TEST_SECTION = 'E2E';
  14  | const TEST_DESCRIPTION = 'E2E smoke test accommodation';
  15  | 
  16  | test.describe('CRUD: Размещения', () => {
  17  |   test('Создание нового размещения через форму @smoke', async ({ page }) => {
  18  |     const form = new AccommodationFormPage(page);
  19  |     await form.gotoCreate();
  20  | 
  21  |     await form.fillName(TEST_NAME);
  22  |     await form.fillAddress(TEST_ADDRESS);
  23  |     await form.fillSection(TEST_SECTION);
  24  |     await form.fillDescription(TEST_DESCRIPTION);
  25  |     await form.fillNumericFields();
  26  |     await form.selectAsset();
  27  |     await form.selectTimezone();
  28  | 
  29  |     await expect(form.submitButton).toBeEnabled({ timeout: 5000 });
  30  | 
  31  |     // Intercept API response
  32  |     const responsePromise = page.waitForResponse(
  33  |       (resp) => resp.url().includes('/admin/accommodations') && resp.request().method() === 'POST',
  34  |       { timeout: 30000 },
  35  |     );
  36  | 
  37  |     await form.submitButton.scrollIntoViewIfNeeded();
  38  |     await form.submit();
  39  | 
  40  |     const response = await responsePromise;
  41  |     expect(response.ok(), `API error: ${response.status()}`).toBeTruthy();
  42  | 
  43  |     // After save — URL changes
  44  |     await page.waitForURL((url) => !url.toString().includes('/create'), { timeout: 30000 });
  45  |   });
  46  | 
  47  |   test('Созданное размещение отображается в списке @smoke', async ({ page }) => {
  48  |     const table = new TablePage(page, '/accommodations');
  49  |     await table.goto();
  50  |     await waitForTableData(page);
  51  | 
  52  |     await table.search(TEST_NAME);
  53  | 
> 54  |     await expect(page.locator('tbody tr')).toHaveCount(1, { timeout: 30000 });
      |                                            ^ Error: expect(locator).toHaveCount(expected) failed
  55  | 
  56  |     const texts = await table.getDataCellTexts(0, 5);
  57  |     expect(texts.some((t) => t.includes(TEST_NAME))).toBeTruthy();
  58  |   });
  59  | 
  60  |   test('Удаление размещения через таблицу @smoke', async ({ page }) => {
  61  |     const table = new TablePage(page, '/accommodations');
  62  |     await table.goto();
  63  |     await waitForTableData(page);
  64  |     await table.search(TEST_NAME);
  65  | 
  66  |     await expect(page.locator('tbody tr')).toHaveCount(1, { timeout: 30000 });
  67  | 
  68  |     const row = page.locator('tbody tr').first();
  69  |     const actionsCell = row.locator('td').last();
  70  |     const deleteBtn = actionsCell.locator('button').first();
  71  |     await deleteBtn.click();
  72  | 
  73  |     // Confirmation in modal — red button
  74  |     const confirmBtn = page.locator('button[class*="red"]');
  75  |     await confirmBtn.waitFor({ state: 'visible', timeout: 5000 });
  76  |     await confirmBtn.click();
  77  | 
  78  |     // After deletion the row disappears
  79  |     await expect(page.locator(`tbody tr:has-text("${TEST_NAME}")`)).toHaveCount(0, {
  80  |       timeout: 15000,
  81  |     });
  82  |   });
  83  | 
  84  |   // Safety net: clean up test data via API if test failed mid-way
  85  |   test.afterAll(async ({ request }) => {
  86  |     try {
  87  |       const api = new ApiHelper(request);
  88  |       await api.authenticate();
  89  |       const data = await api.getAccommodations();
  90  |       const items = (data?.data ?? data ?? []) as Array<{ id: number; name?: string }>;
  91  |       const testIds = items.filter((i) => i.name?.startsWith(TEST_PREFIX)).map((i) => i.id);
  92  | 
  93  |       if (testIds.length > 0) {
  94  |         await api.deleteAccommodations(testIds);
  95  |       }
  96  |     } catch {
  97  |       // Cleanup — best effort
  98  |     }
  99  |   });
  100 | });
  101 | 
```